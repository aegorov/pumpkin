<h1>Список котельных</h1>

<table>
  <tr>
    <th>Имя</th>
    <th>Номер</th>
    <th></th>
    <th></th>
  </tr>

<% @boiler_rooms.each do |boiler_room| %>
  <tr>
    <td><%= boiler_room.name %></td>
    <td><%= boiler_room.number %></td>
    <td><%= link_to 'Редактировать', edit_boiler_room_path(boiler_room) %></td>
    <td><%= link_to 'Удалить', boiler_room, :confirm => 'Вы уверены?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить котельную', new_boiler_room_path %>
