<%= form_for(@boiler_room) do |f| %>
  <% if @boiler_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boiler_room.errors.count, "error") %> prohibited this boiler_room from being saved:</h2>

      <ul>
      <% @boiler_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="clearfix">
    	<%= f.label :name, "Имя" %>
	  <div class="input">
    	<%= f.text_field :name %>
	</div>
  </div>
  <div class="clearfix">
    	<%= f.label :number, "Номер"%>
  	<div class="input">
    	<%= f.number_field :number %>
	</div>
  </div>
  <div class="actions">
	<% if create == true %>
		<%= f.submit 'Создать', :class => "btn info"%>
	<% else %>	
		<%= f.submit 'Обновить', :class => "btn info"%>
	<% end %>
  </div>
<% end %>
