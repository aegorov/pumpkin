<%= form_for(@boiler_room) do |f| %>
  <% if @boiler_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boiler_room.errors.count, "error") %> prohibited this boiler_room from being saved:</h2>

      <ul>
      <% @boiler_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Имя" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number, "Номер"%><br />
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
